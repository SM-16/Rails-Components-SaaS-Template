
<%= render "static_pages/navbar"%>

<%# Home %>
<section class="min-h-screen flex flex-col lg:mt-0 items-center lg:gap-10 gap-20 gradient-pattern square-pattern">
    <%= render "static_pages/home_sections/section_1"%>
    <%= render "static_pages/home_sections/components_preview"%>
    
    <%= render "static_pages/home_sections/section_2"%>
    <%= render "static_pages/home_sections/story"%>
    <%= render "static_pages/home_sections/pricing_section"%>
    <%= render "static_pages/home_sections/section_3"%>
    <%= render "static_pages/home_sections/FAQ"%>
    <%= render "static_pages/footer"%>
</section>
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
<script>
$(document).ready(function(){
  // Add smooth scrolling to all links
  $("a").on('click', function(event) {

    // Make sure this.hash has a value before overriding default behavior
    if (this.hash !== "") {
      // Prevent default anchor click behavior
      event.preventDefault();

      // Store hash
      var hash = this.hash;

      // Using jQuery's animate() method to add smooth page scroll
      // The optional number (800) specifies the number of milliseconds it takes to scroll to the specified area
      $('html, body').animate({
        scrollTop: $(hash).offset().top
      }, 800, function(){
   
        // Add hash (#) to URL when done scrolling (default click behavior)
        window.location.hash = hash;
      });
    } // End if
  });
});
</script>